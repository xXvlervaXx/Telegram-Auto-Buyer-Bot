# Telegram Auto Buyer Bot

Этот бот предназначен для автоматического мониторинга и покупки товаров в другом Telegram-боте. Он работает в полностью автономном режиме.

## 🚀 Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/ВАШ_НИК/ИМЯ_РЕПОЗИТОРИЯ.git
    cd ИМЯ_РЕПОЗИТОРИЯ
    ```

2.  **Установите зависимости:**
    (Рекомендуется делать это в виртуальном окружении)
    ```bash
    pip install -r requirements.txt
    ```

3.  **Создайте файл конфигурации:**
    Скопируйте `config.ini.example` в новый файл с названием `config.ini`.
    ```bash
    cp config.ini.example config.ini
    ```

4.  **Заполните `config.ini`:**
    Откройте файл `config.ini` в текстовом редакторе и вставьте свои данные:
    - `api_id` и `api_hash`: их можно получить на [my.telegram.org](https://my.telegram.org).
    - Остальные параметры можно оставить по умолчанию или настроить по желанию.

5.  **Запустите бота:**
    ```bash
    python main.py
    ```
    - При первом запуске скрипт попросит вас ввести номер телефона, код из Telegram и пароль двухфакторной аутентификации (если он включен).
    - Он также автоматически создаст в вашем аккаунте приватный канал для логов (`Bakery Bot Logs`).

## ⚙️ Как это работает

-   Бот раз в минуту (настраивается в `config.ini`) опрашивает заданную категорию товаров.
-   Он сканирует все появившиеся кнопки и сравнивает их `callback_data` со списком игнорируемых.
-   Любая кнопка, которой нет в списке игнорируемых, считается товаром.
-   При обнаружении нового `callback_data`, бот отправляет уведомление в лог-канал.
-   При обнаружении товара, бот автоматически запускает процедуру покупки, нажимая на кнопки методом исключения.
-   Результаты всех покупок (успех или ошибка) отправляются в лог-канал.